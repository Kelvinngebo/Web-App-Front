# Frontend scaffold for Fraud Ad Detector

This is a minimal Vite + React scaffold.

Quick start (from repo root):

cd frontend
npm install
npm run dev

Set VITE_API_URL environment variable to your backend URL before building or running in production.
